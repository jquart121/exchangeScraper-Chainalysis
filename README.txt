Before running the .py file, install python-binance and coinbase (pip install python-binance
and pip install coinbase) if not already installed.

Run the python file and the resulting output should be the html file. 
Click the link and the webpage should appear where the data and
recommendations should be located.